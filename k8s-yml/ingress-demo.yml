apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  creationTimestamp: null
  generation: 1
  name: demo-localhost
  namespace: k1s
spec:
  ingressClassName: nginx
  rules:
  - host: demo.localdev.me
    http:
      paths:
      - backend:
          service:
            name: demo
            port:
              number: 80
        path: /order/.*/checkout
        pathType: Prefix
status:
  loadBalancer: {}